/** Parses raw user input into a standardized format for processing.*/
public class Parser {
    /**
     * Returns the trimmed version of the input string.
     *
     * @param input Raw user input.
     * @return Trimmed input string.
     */
    public static String parse(String input) {
        return input.trim();
    }
}
